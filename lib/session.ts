// lib/session.ts
export async function getCurrentUser() {
  // Simule un utilisateur connecté (que je dois remplacer ma vrai session)
  return { id: 1, email: "user@bienloue.fr", role: "tenant" }
}
